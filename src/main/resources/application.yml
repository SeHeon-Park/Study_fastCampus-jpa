spring:
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  datasource:
    url: jdbc:mysql://localhost:3306/book_manager
    username: root
    password: sonamu0928*

logging:
  level:
    root: trace

